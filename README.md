# Overlord API

Esta é uma API simples para gerenciar personagens do anime Overlord, construída com Express.js.

## Funcionalidades

- Listar todos os personagens
- Buscar personagem por ID
- Criar novo personagem
- Atualizar personagem existente
- Remover personagem

## Endpoints

- `GET /personagens` - Lista todos os personagens
- `GET /personagens/:id` - Busca personagem por ID
- `POST /personagens` - Cria novo personagem
- `PUT /personagens/:id` - Atualiza personagem
- `DELETE /personagens/:id` - Remove personagem

## Como executar

1. Instale as dependências: `npm install`
2. Execute o servidor: `npm start`
3. Acesse em `http://localhost:3000`

## Imagem do Rei Supremo

![Ainz Ooal Gown](ainz.jpg)

*Adicione uma imagem chamada `ainz.jpg` na raiz do projeto para exibir aqui.*